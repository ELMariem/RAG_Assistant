# --- Web framework ---
fastapi==0.140.13
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4

# --- Vector store + embeddings/reranking ---
chromadb==1.5.9
sentence-transformers==3.4.1
transformers==4.57.6

# --- Document ingestion ---
docling==2.113.0
PyMuPDF==1.28.0
Pillow==12.3.0

# --- LLM backends ---
ollama==0.6.2
groq==1.6.0

# --- Auth ---
PyJWT==2.10.1
bcrypt==5.0.0

# --- SQL Server (chat/user history) - needs msodbcsql17 at OS level, see Dockerfile ---
SQLAlchemy==2.0.36
pyodbc==5.1.0

# --- Misc ---
python-dotenv==1.2.2
numpy==2.2.6

# --- Torch: CPU-only build, needs the extra index in the Dockerfile's pip install line ---
torch==2.13.0+cpu